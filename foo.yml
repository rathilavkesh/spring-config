lavkesh:
  application:
    name: Spring Cloud Demo 1
    
feign:
  hystrix:
    enabled: false

#Loging config
logging:
  level:
    org:
      springframework:
        web: DEBUG
      netflix:
        hystrix: DEBUG
